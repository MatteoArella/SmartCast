<%= form_for(@episode, as: :video_episode, :url => { :action => "update" }, html: { :multipart => true, id: "input-container", class: "cf wrapper"}) do |f| %>
  <div id="image-upload">
    <%= f.file_field :image %>
  </div>
  <div id="title">
    <%= f.text_field :title, placeholder: "Insert Episode title", class: "border-animation" %>
  </div>
  <div id="description" >
    <%= f.text_area :description, placeholder: "Insert Episode description", class: "border-animation" %>
  </div>

  <div class="btn-bar">
    <div class="field">
      <%= f.file_field :mp4 %>
    </div>
    <div class="right">
      <%= f.submit "Update Episode", class: "button button-highlight" %>
      <%= link_to "Cancel", :back, class: "button button-white" %>
    </div>
  </div>
<% end %>