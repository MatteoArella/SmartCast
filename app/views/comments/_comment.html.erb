<h2><li id="comment<%= comment.id %>">
  <span class="content"><%= comment.content %></span></h2>
  <div ><span class="timestamp">
    Posted <%= time_ago_in_words(comment.created_at) %> ago by <%=comment.user.username %>.</div>
<% if current_user == comment.user or current_user.admin?%>
<%= link_to "Delete", comment, method: :delete,
                                  data: { confirm: "You sure?" } %>


<%= link_to "Edit", edit_comment_path(comment) %>

<% end%>
  </span>
</li>